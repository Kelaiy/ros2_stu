launch:
# 声明参数
- arg:
    name: "bg_b"
    default: "0"
# 调用参数
- node:
    pkg: "turtlesim"
    exec: "turtlesim_node"
    param:
    -
        name: "background_r"
        value: $(var bg_b)

# 通过执行launch导入参数


# 使用 ros2 launch cpp1_launch yaml4_args_launch.yaml bg_b:=255